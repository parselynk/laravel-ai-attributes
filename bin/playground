#!/usr/bin/env bash
#
# Open a Tinker REPL with the package loaded and your .env variables exported.
# Usage: ./bin/playground

set -e

cd "$(dirname "$0")/.."

if [ -f .env ]; then
    set -a
    # shellcheck disable=SC1091
    source .env
    set +a
fi

exec vendor/bin/testbench tinker "$@"
